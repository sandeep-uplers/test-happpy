'use client';

/**
 * OAuth redirect target for @react-oauth/google auth-code popup flow.
 * The library completes the handshake on this URL inside the popup window.
 */
export default function GoogleAuthCallbackPage() {
    return null;
}
