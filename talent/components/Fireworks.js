'use client';

// import { Fireworks } from '@fireworks-js/react'

export default function FireWorksCelebrration() {

    return (
        <div style={{ position: 'fixed', top: 0, left: 0, width: '100%', height: '100%',zIndex:'99999' }}>
            {/* <Fireworks autostart={true} style={{ width: '100%', height: '100%' }} options={{
                mouse: {
                    click: false,
                    move: false,
                    max: 1
                }
            }} /> */}
        </div>
    )
}
